import React from "react";
import { DEFAULT_MAIN_BG } from "@/lib/constants";

export default function Footer() {
  return (
    <footer className={DEFAULT_MAIN_BG}>
      <div className="container mx-auto text-center">Footer</div>
    </footer>
  );
}
